- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~./config']

- link:
    ~/.bashrc:
    ~/.tmux.conf:
    ~/.vimrc:
    ~/.vim:
    ~/.dotfiles: ''
    ~/.zshrc:

- shell:
    - [git submodule update --init --recursive, Installing submodules]

