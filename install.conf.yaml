- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~./config']

- link:
    ~/.bash:
    ~/.bashrc:
    ~/.tmux:
    ~/.tmux.conf:
    ~/.shell:
    ~/.vimrc:
    ~/.vim:
    ~/.dotfiles: ''
    ~/.zsh:
    ~/.zshrc:
    ~/.config/nvim/init.vim: config/nvim/init.vim
- shell:
    - [git submodule update --init --recursive, Installing submodules]

