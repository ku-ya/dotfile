- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~./config']

- link:
    ~/.bash:
    ~/.bashrc:
    ~/.tmux.conf:
    ~/.shell:
    ~/.vimrc:
    ~/.vim:
    ~/.dotfiles: ''
    ~/.zsh:
    ~/.zshrc:

- shell:
    - [git submodule update --init --recursive, Installing submodules]

